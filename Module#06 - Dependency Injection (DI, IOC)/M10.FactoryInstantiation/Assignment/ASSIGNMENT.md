ASSIGNMENT — M10.FactoryInstantiation

1. لماذا نستخدم factories بدلاً من تسجيل النوع مباشرة؟
2. اذكر مثالًا حيث يتطلب إنشاء الكائن منطق تشغيل (configuration, environment) قبل الإنشاء.
3. نفّذ أو عدّل factory لتمرير معطيات بناءً على إعداد (config).
