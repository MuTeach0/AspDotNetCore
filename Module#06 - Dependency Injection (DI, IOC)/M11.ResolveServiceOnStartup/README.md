# M11.ResolveServiceOnStartup

هدف المشروع: إظهار حل الاعتماديات أثناء بدء تشغيل التطبيق (resolve on startup) لأغراض التهيئة.

تشغيل المشروع:

```powershell
dotnet run --project ./M11.ResolveServiceOnStartup/M11.ResolveServiceOnStartup.csproj
```

ملفات مهمة:
- `Program.cs` — قد يستدعي بعض الخدمات عند startup.

راجع `Assignment/ASSIGNMENT.md` لأسئلة الفهم.
