# M09.KeyedServices

هدف المشروع: استخدام خدمات مرمّزة (keyed services) لتحديد تنفيذ معين عند الطلب.

تشغيل المشروع:

```powershell
dotnet run --project ./M09.KeyedServices/M09.KeyedServices.csproj
```

ملفات مهمة:
- `Program.cs` — أمثلة على كيفية طلب خدمة بـ key.

راجع `Assignment/ASSIGNMENT.md` لأسئلة الفهم.
