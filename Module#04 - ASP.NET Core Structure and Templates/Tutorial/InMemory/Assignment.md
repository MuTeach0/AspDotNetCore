Assignment — InMemory

1) شغّل التطبيق واستعلم عن `/MaxConnections`. ماذا تظهر؟

2) عدّل `Program.cs` لإضافة مفتاح جديد في الذاكرة (مثال: `SupportMode = true`) ثم أعد التشغيل وتحقق.

3) ناقش متى يكون من المناسب استخدام مزود الإعدادات من الذاكرة بدلاً من ملف التهيئة.