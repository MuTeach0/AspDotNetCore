Assignment — FirstASPProjectVSCode

1) أرسل طلب GET إلى `/authors/ali?lang=ar` وسجّل الاستجابة JSON. ما قيمة `RouteValues` و`Query`؟

2) أرسل طلب POST مع JSON body إلى نفس المسار. ماذا يظهر في `Body` في الاستجابة؟

3) عدّل الكود ليضيف حقلًا جديدًا في الاستجابة يضم زمن الاستجابة (latency) وأعد الاختبار.

4) صف استخدامات عملية لهذا endpoint في تطبيق حقيقي (debugging, request inspection, logging).