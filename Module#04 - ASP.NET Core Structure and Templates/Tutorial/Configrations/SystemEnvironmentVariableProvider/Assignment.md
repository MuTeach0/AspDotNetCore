Assignment — SystemEnvironmentVariableProvider

1) عيّن متغير بيئة `ServiceName` ثم شغّل التطبيق واطلب `/ServiceName`. ماذا ظهر؟

2) إذا كان نفس المفتاح موجودًا في `appsettings.json` ومتغير البيئة، أيهما يأخذ الأولوية؟ جرّب ذلك ودوّن النتيجة.

3) صف حالات استخدام حقيقية حيث تفضّل متغيرات البيئة (مثل في الحاويات، CI/CD، أو سرّيات بسيطة).

4) أضف ملاحظة حول الأمان: لماذا لا يجب وضع أسرار حساسة في `appsettings.json` في البيئات الحقيقية؟