Assignment — FileProviders

1) افتح `mycustomeconfig.json` و`config.INI` ودوّن مثالاً على مفتاح موجود في كل ملف.

2) أعد تشغيل التطبيق ثم اطلب المفتاح من endpoint المناسب (`/ini/{key}` أو `/{key}`).

3) جرّب تعديل `mycustomeconfig.json` أثناء تشغيل التطبيق ولاحظ ما إذا تغيرت القيمة عند استدعاء الـ endpoint (بفضل `reloadOnChange`).

4) أضف ملف تكوين جديد من نوع XML أو YAML (شرح موجز كيف) وادمجه في `Program.cs`، ثم دوّن ما واجهته من صعوبات أو نجاحات.