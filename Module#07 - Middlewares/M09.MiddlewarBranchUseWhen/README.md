# M09.MiddlewarBranchUseWhen

مثال يوضّح استخدام `UseWhen` لتطبيق middlewares على شروط محددة داخل نفس الـ pipeline.

تشغيل
1. `cd "M09.MiddlewarBranchUseWhen"`
2. `dotnet run`

نقاط مهمة
- `UseWhen` يسمح بتطبيق مجموعة من middlewares فقط عندما يتحقق شرط.
