# Assgnment — M03.CreateMiddlewareRun

أسئلة وتمارين:

1) لماذا لا يُتوقع استدعاء `next()` داخل `Run`؟ ومتى يكون هذا مرغوبًا؟
2) مهمّة: غيّر `Run` ليطبع رسالة في الـ console قبل إرسال الاستجابة، ثم أضف `Use` قبلها ليطبع رسالة أخرى — رصد ترتيب الطباعة.
3) اختبر ما الذي سيظهر في الـ console عند طلبين متتالين.
