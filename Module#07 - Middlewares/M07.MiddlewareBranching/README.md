# M07.MiddlewareBranching

مثال يوضح كيفية عمل فرع داخل خط الـ middleware (branching) باستخدام `Map` أو شروط مسار.

تشغيل
1. `cd "M07.MiddlewareBranching"`
2. `dotnet run`

انتبه لــ:
- كيف يتم توجيه الطلبات إلى فروع مختلفة بناءً على المسار (path).
